# Core dependencies
numpy>=1.20.0
scipy>=1.7.0
librosa>=0.9.0
soundfile>=0.10.0
pyloudnorm>=0.1.0
psutil>=5.8.0  # For GPU memory monitoring

# Optional: GPU acceleration (Apple Silicon only)
# Requires macOS 12.3+ and Apple M1/M2/M3/M4 chip
# Provides 10-27x speedup for audio analysis
torch>=1.12.0; platform_system == "Darwin" and platform_machine == "arm64"

# Optional: Testing
pytest>=7.0.0
